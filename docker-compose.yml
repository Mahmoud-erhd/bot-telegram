version: "3.8"

services:
  telegram-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: m-automation-store-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot_data:/data

volumes:
  bot_data:
    driver: local
